/*
const jsdom = require("jsdom");
const { JSDOM } = jsdom;
global.$ = global.jQuery = require('jquery');
const assert = require("chai").asset;
const timer = require("../js/timer");

describe("Timer", function(){
	it("When setLength(25) is executed getLength() should return 25.", function(){
		timer.setLength(25);
		assert.equal(timer.getLength(), 25);
	
	});
});

*/